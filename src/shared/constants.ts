export const DEFAULT_PROJECT_ROOT = 'D:/Projects';

export const DEFAULT_START_COMMAND = 'npm run dev';

export const DEFAULT_LOG_LINE_LIMIT = 500;

export const LOCAL_URL_PATTERN = /(https?:\/\/(?:localhost|127\.0\.0\.1|0\.0\.0\.0|[\d.]+):\d+[^\s]*)/i;
