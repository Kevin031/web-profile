# Web Profile 简明操作手册

Web Profile 是 Windows 前端项目启动器，可以集中查看、启动和管理本机前端项目。

## 它解决了哪些问题

同时维护多个前端项目时，常见问题包括：

- 项目分散在不同目录，查找和切换成本高。
- 每次都要打开终端、进入目录并输入启动命令。
- 多个开发服务同时运行时，难以快速确认运行状态和访问地址。
- 需要频繁切换编辑器、终端、文件资源管理器和浏览器。
- Git 分支、未提交修改等状态分散在不同终端中，不方便集中查看。
- 关闭工作环境时，容易遗漏仍在后台运行的项目进程。

Web Profile 将项目查找、启动、停止、访问、Git 状态和常用工具入口集中到一个工作台中，减少重复操作。

## 核心优势

### 集中管理多个项目

扫描指定目录后，可以在同一列表中搜索、筛选和管理所有前端项目，不需要逐个打开文件夹。

### 一键启动和停止

每个项目可以保存独立的启动命令。后续只需点击按钮即可启动、停止或重启，也可以一次停止所有运行中的项目。

### 自动识别项目信息

Web Profile 会读取 `package.json` 和锁文件，展示项目脚本并识别 npm、pnpm、yarn 或 bun，减少手动配置。

### 集成常用开发入口

可以直接用文件资源管理器、VS Code、Cursor 或终端打开项目。启动日志中出现本地 URL 后，还可以直接打开浏览器访问。

### 集中查看运行和 Git 状态

项目列表会展示运行状态、访问地址、当前分支和工作区状态，并支持刷新 Git、切换本地分支和执行 `Pull`。

### 本机运行，权限边界清晰

项目和配置保留在本机。Web Profile 只对已扫描的项目执行受控操作，不向界面开放通用 Shell 或任意文件系统能力。

## 使用前准备

使用 Web Profile 前，请确认电脑已安装：

- Node.js
- Git
- 项目使用的包管理器，例如 npm、pnpm、yarn 或 bun
- 可选：VS Code 或 Cursor

项目应包含 `package.json`。

## 首次使用

1. 启动 Web Profile。
2. 首次打开时，选择存放前端项目的根目录。
3. 等待应用扫描项目。
4. 项目出现后，即可进行搜索、启动和管理。

项目目录示例：

```text
D:/Projects/
├── project-a/
│   └── package.json
└── project-b/
    └── package.json
```

Web Profile 只识别根目录下包含 `package.json` 的直接子目录，不会继续扫描更深层级。

## 查找项目

左侧提供以下功能：

- 在搜索框中输入项目名称或路径。
- 选择“全部”查看所有项目。
- 选择“收藏”查看已收藏项目。
- 选择“运行中”查看正在运行的项目。
- 选择“有改动”查看存在未提交修改的 Git 项目。
- 选择“隐藏”查看已隐藏项目。
- 使用工作台右上角的视图按钮切换表格或网格展示。网格卡片同样支持收藏、打开、启动、停止、Git 和 URL 操作。

应用会保存当前选中的展示方式，下次启动时自动恢复。

点击顶部刷新按钮，可以重新扫描项目目录。

## 启动项目

1. 在项目列表中选择项目。
2. 在右侧确认启动命令。
3. 启动命令不正确时，修改后点击“保存”。
4. 点击项目行中的启动按钮。
5. 等待状态变为“运行中”。

常见启动命令：

```text
npm run dev
pnpm dev
yarn dev
bun dev
```

启动成功后，如果项目日志输出了本地访问地址，可以点击列表中的 URL 或右侧“访问”按钮在浏览器中打开。

## 停止和重启项目

- 点击停止按钮，结束当前项目。
- 点击重启按钮，重新运行当前项目。
- 点击顶部“全部停止”，结束所有正在运行的项目。
- 关闭 Web Profile 时，如果仍有项目运行，选择“停止并退出”可结束项目后关闭应用。

## 运行其他脚本

选择项目后，右侧会显示 `package.json` 中配置的脚本。

点击某个脚本后，Web Profile 会使用项目对应的包管理器运行该脚本。

例如：

```json
{
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "build": "vite build"
  }
}
```

界面中会提供 `dev`、`test` 和 `build` 等脚本入口。

## 打开项目

点击项目行或详情区中的打开按钮，可以使用以下工具打开项目：

- 文件资源管理器
- VS Code
- Cursor
- 终端

通过打开按钮旁的下拉菜单切换工具。选择后，其他项目也会默认使用该工具。

## Git 操作

Git 项目支持以下操作：

- 刷新 Git 状态。
- 查看当前分支。
- 查看工作区是否存在未提交修改。
- 在右侧切换本地分支。
- 点击 `Pull` 拉取当前分支。

切换分支或拉取失败时，可以打开项目终端并运行：

```powershell
git status
```

根据输出处理未提交修改、冲突或登录认证问题。

## 收藏和隐藏项目

### 收藏项目

点击项目前的收藏按钮。收藏后的项目会优先显示，也可以通过左侧“收藏”筛选查看。

### 隐藏项目

1. 选择需要隐藏的项目。
2. 点击右侧详情区顶部的隐藏按钮。

隐藏项目不会删除电脑中的项目文件。

### 恢复隐藏项目

1. 点击左侧“隐藏”。
2. 选择需要恢复的项目。
3. 再次点击右侧详情区顶部的隐藏按钮。

## 查看日志

选择项目后，右侧日志区域会显示：

- 启动命令
- 项目输出
- 错误信息
- 进程状态

项目启动失败时，应先查看日志中的第一条错误信息。

## 常见问题

### 项目没有出现在列表中

检查以下内容：

1. 项目是否位于所选根目录的直接下一级。
2. 项目目录中是否存在有效的 `package.json`。
3. 项目是否被隐藏。
4. 点击顶部刷新按钮重新扫描。

### 项目无法启动

检查以下内容：

1. 启动命令是否正确。
2. 项目依赖是否已经安装。
3. Node.js 和项目使用的包管理器是否可用。
4. 项目需要的端口是否已被占用。
5. 右侧日志是否显示具体错误。

也可以打开项目终端，手动执行界面中的启动命令进行排查。

### 启动后没有访问地址

Web Profile 需要从项目日志中识别本地地址，例如：

```text
Local: http://localhost:5173/
```

如果项目没有输出地址，请根据项目使用的端口手动在浏览器中访问。

### 无法使用 VS Code 或 Cursor 打开

确认对应软件已经安装。仍然无法打开时，可以切换为文件资源管理器或终端。

### Git 操作失败

确认：

- 电脑已安装 Git。
- 当前项目是 Git 仓库。
- 项目没有阻止切换分支的未提交修改。
- 当前账号有远程仓库访问权限。

## 后续更新计划

以下内容为规划方向，具体范围和上线顺序可能根据实际使用反馈调整。

### 近期计划

- 支持单独添加不在项目根目录中的项目。
- 增加更完整的应用设置界面，例如日志数量、启动扫描和默认打开工具。
- 改进项目扫描结果，明确展示无法识别或 `package.json` 异常的原因。
- 增强启动失败提示，帮助定位依赖缺失、命令不可用和端口占用问题。

### 中期计划

- 支持项目分组、标签和更多排序方式。
- 支持批量启动、批量刷新 Git 等多项目操作。
- 增加常用命令模板，降低新项目的配置成本。
- 完善日志搜索、过滤、复制和导出能力。

### 长期方向

- 支持工作区方案，一键恢复一组项目及其启动顺序。
- 增加项目健康检查，集中展示依赖、Git 和运行环境问题。
- 在保证本机安全边界的前提下，扩展更多编辑器和开发工具集成。
